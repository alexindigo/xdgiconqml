#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_DIR="$(dirname "$SCRIPT_DIR")"

# Clean build dirs, then rebuild (release)
"${SCRIPT_DIR}/clean"
"${SCRIPT_DIR}/build" "$@"
